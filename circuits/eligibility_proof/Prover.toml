# MedVault Eligibility Proof — Prover inputs (see circuits/eligibility_proof/src/main.nr)
#
# Private witnesses include patient profile fields, Semaphore secret, staged FHE handle,
# decrypted_eligible (encrypted-criteria mode), and criteria_binding_hash echo.
#
# Public inputs (17): scope, nullifier, profile_commitment, result_hash, eligible,
#   fhe_stage_handle_hash, criteria_schema_hash, min_age (or binding echo at idx 7),
#   max_age .. requires_normal_bp, criteria_mode (0=plaintext, 1=encrypted echo).

secret = "12345678901234567890"
scope_internal = "42"
commitment = "555"
age = 30
gender = true
weight = 70
height = 175
has_diabetes = false
hb_level = 140
is_smoker = false
has_hypertension = false
staged_fhe_handle = "999"
decrypted_eligible = "1"
criteria_binding_hash = "0"

scope = "1"
nullifier = "0"
profile_commitment = "0"
result_hash = "0"
eligible = "1"
fhe_stage_handle_hash = "999"
criteria_schema_hash = "1"
min_age = "18"
max_age = "65"
requires_diabetes = "0"
min_hb = "120"
gender_requirement = "0"
min_height = "0"
max_weight = "0"
requires_non_smoker = "0"
requires_normal_bp = "0"
criteria_mode = "0"
