{
    "name": "medvault-subgraph",
    "version": "0.1.22",
    "license": "UNLICENSED",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ medvault --version-label v0.1.0",
        "deploy:studio": "graph deploy --node https://api.studio.thegraph.com/deploy/ medvault --version-label v0.1.0",
        "create-local": "graph create --node http://localhost:8020/ medvault-subgraph",
        "remove-local": "graph remove --node http://localhost:8020/ medvault-subgraph",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 medvault-subgraph"
    },
    "dependencies": {
        "@graphprotocol/graph-ts": "0.31.0"
    },
    "devDependencies": {
        "@graphprotocol/graph-cli": "^0.98.1"
    }
}

